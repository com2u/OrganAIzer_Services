# Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# Backend API Key (for all API calls)
# This should match the API key configured in your backend
VITE_API_KEY=your_api_key_here

# Backend API URL (for development)
# Use localhost for local development
VITE_API_URL=http://localhost:8000/api

# Production Backend API URL (for production builds)
# This will be used when building for production
VITE_API_URL_PROD=https://organaizer_backend.com2u.selfhost.eu

# Note: The vite.config.ts will automatically use:
# - VITE_API_URL for development (npm run dev)
# - VITE_API_URL_PROD for production builds (npm run build)
# - The API key is embedded at build time via VITE_API_KEY